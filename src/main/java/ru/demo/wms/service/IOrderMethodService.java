package ru.demo.wms.service;

import java.util.List;
import java.util.Map;

import ru.demo.wms.model.OrderMethod;

public interface IOrderMethodService {

	Integer saveOrderMethod(OrderMethod om);
	void updateOrderMethod(OrderMethod om);
	void deleteOrderMethod(Integer id);
	
	OrderMethod getOneOrderMethod(Integer id);
	List<OrderMethod> getAllOrderMethods();
	boolean isOrderMethodCodeExist(String code);
	boolean isOrderMethodCodeExistForEdit(String code,Integer id);
	
	List<Object[]> getOrderMethodModeAndCount();
	Map<Integer,String> getOrderMethodIdAndCode();
}
/*
Интерфейс IOrderMethodService определяет набор операций для управления методами заказа в системе управления логистикой или складом. Эти методы предоставляют функциональность для создания, обновления, удаления и извлечения информации о методах заказа, а также для выполнения специфических проверок и агрегации данных. Давайте подробнее рассмотрим каждую из предложенных операций:

Методы интерфейса IOrderMethodService:
Integer saveOrderMethod(OrderMethod om);
Сохраняет новый или обновленный метод заказа в базе данных. Этот метод принимает объект OrderMethod, который содержит всю необходимую информацию о методе заказа, и возвращает идентификатор сохраненного объекта.

void updateOrderMethod(OrderMethod om);
Обновляет существующий метод заказа. Этот метод также принимает объект OrderMethod и обновляет соответствующую запись в базе данных. В случае, если объект не существует, может потребоваться генерация исключения или другая форма обработки ошибки.

void deleteOrderMethod(Integer id);
Удаляет метод заказа по его идентификатору. Это действие необратимо удаляет запись из базы данных.

OrderMethod getOneOrderMethod(Integer id);
Возвращает один метод заказа по его идентификатору. Может использоваться для получения детальной информации о конкретном методе заказа.

List<OrderMethod> getAllOrderMethods();
Возвращает список всех методов заказа, доступных в системе. Может быть полезно для отображения в пользовательском интерфейсе или для административных целей.

boolean isOrderMethodCodeExist(String code);
Проверяет, существует ли метод заказа с заданным кодом. Эта функция может использоваться для валидации уникальности кода метода заказа при создании или обновлении записей.

boolean isOrderMethodCodeExistForEdit(String code, Integer id);
Подобно предыдущему методу, проверяет существование метода заказа с заданным кодом, исключая запись с определенным идентификатором. Это полезно для валидации кода при редактировании существующих методов заказа.

List<Object[]> getOrderMethodModeAndCount();
Агрегирует данные по методам заказа, возвращая количество методов для каждого режима. Это может быть использовано для аналитических отчетов и визуализаций.

Map<Integer, String> getOrderMethodIdAndCode();
Возвращает карту, содержащую идентификаторы и коды всех методов заказа. Это удобно для использования в выпадающих списках и других элементах интерфейса, где требуется предоставить пользователям возможность выбора из существующих методов заказа.

Реализация этого интерфейса должна обеспечить эффективное и надежное управление методами заказа, позволяя системе поддерживать разнообразные способы обработки заказов и предоставлять пользователю гибкие инструменты для оптимизации процессов заказа.
*/